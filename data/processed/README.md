Default output path:

```text
data/processed/jena_climate_hourly.csv
```

Default preprocessing metadata path:

```text
data/processed/jena_climate_hourly_metadata.json
```

These generated files are local run artifacts and should be recreated from the
current preprocessing pipeline rather than treated as authoritative repo data.
